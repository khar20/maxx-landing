---
const cardItems = [
  {
    id: 'web-development',
    title: 'Desarrollo de Soluciones Web',
    description:
      'Creamos páginas personalizadas e impactantes que capturan la atención de tu público objetivo, maximizan las conversiones y reflejan la esencia de tu marca.',
    image: 'https://placehold.co/400x300',
    url: '/web-development',
    transitionName: 'img1',
  },
  {
    id: 'desktop-applications',
    title: 'Desarrollo de Aplicaciones de Escritorio',
    description:
      'Desarrollamos software personalizado y funcional adaptado a las necesidades de tu empresa.',
    image: 'https://placehold.co/400x300',
    url: '/app-development',
    transitionName: 'img2',
  },
];
---

<div class="max-w-6xl mx-auto px-4 py-12 space-y-10">
  {cardItems.map((card) => (
    <div
      class="relative bg-white dark:bg-gray-900 rounded-xl overflow-hidden shadow-lg flex flex-col md:flex-row group"
    >
      <!-- Imagen con corte diagonal -->
      <div class="relative w-full md:w-1/2 h-64 md:h-auto overflow-hidden">
        <img
          src={card.image}
          alt={card.title}
          class="w-full h-full object-cover"
        />
        <!-- Capa diagonal -->
        <div
          class="absolute top-0 right-0 w-full h-full bg-gradient-to-tr from-white/30 dark:from-gray-900/30 via-transparent to-transparent"
          style="clip-path: polygon(0 0, 100% 0, 65% 100%, 0% 100%);"
        ></div>
      </div>

      <!-- Contenido -->
      <div class="w-full md:w-1/2 p-8 flex flex-col justify-center text-center md:text-left bg-white dark:bg-gray-900">
        <h3 class="text-2xl font-bold text-gray-800 dark:text-white mb-2">{card.title}</h3>
        <p class="text-gray-600 dark:text-gray-300 mb-6 leading-relaxed">
          {card.description}
        </p>
        <a
          href={card.url}
          class="inline-flex items-center gap-2 self-center md:self-start px-6 py-3 bg-gradient-to-r from-cyan-500 to-blue-700
                 hover:from-cyan-600 hover:to-blue-800 dark:from-cyan-400 dark:to-blue-600
                 text-white font-medium rounded-lg shadow-md transition-all duration-300
                 hover:shadow-[0_0_12px_rgba(79,195,247,0.6)]"
        >
          <span>Más información</span>
          <svg
            class="w-4 h-4 transition-transform duration-300 group-hover:translate-x-1"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>
    </div>
  ))}
</div>
