---
import Image from 'astro/components/Image.astro';
import CheckCircle from './icons/CheckCircle.astro';

const list = [
	'Motivación por aprender y mejorar en cada tarea',
	'Conocimientos sólidos adquiridos en formaciones recientes',
	'Buena comunicación y actitud colaborativa',
	'Flexibilidad para adaptarse a diferentes equipos y proyectos',
];
---

<div class="container px-4 md:px-6 py-12">
  <div class="grid gap-10 md:gap-16 lg:grid-cols-2 items-center">
    
    <!-- Texto -->
    <div class="flex flex-col justify-center space-y-6">
      <div class="space-y-4">
        <h2
          class="text-3xl sm:text-4xl md:text-5xl font-bold tracking-tight
          bg-clip-text text-transparent
          bg-gradient-to-r from-blue-700 to-blue-900
          dark:from-cyan-400 dark:to-blue-700 dark:bg-gradient-to-r
          pb-2"  
        >
          Apasionados por la tecnología
        </h2>

        <p class="text-gray-600 dark:text-gray-300 text-base md:text-lg max-w-[600px]">
          Somos un equipo de desarrolladores que sienten verdadera
          pasión por dar vida a soluciones digitales que marcan la
          diferencia.
        </p>
      </div>

      <!-- Lista -->
      <ul class="space-y-3">
        {list.map((item) => (
          <li class="flex items-start gap-3 text-gray-800 dark:text-gray-200 text-sm md:text-base">
            <CheckCircle class="h-5 w-5 text-blue-600 dark:text-blue-400 shrink-0 mt-0.5" />
            <span>{item}</span>
          </li>
        ))}
      </ul>
    </div>

    <!-- Imagen -->
    <div class="relative w-full max-w-[550px] h-auto">
      <div class="rounded-xl overflow-hidden shadow-xl ring-1 ring-gray-100 dark:ring-gray-800 w-full h-full">
        <Image
          alt="Team working together"
          loading="lazy"
          width="550"
          height="400"
          decoding="async"
          class="rounded-xl object-cover brightness-100 dark:brightness-90 w-full h-full"
          src="https://placehold.co/550x400"
          style="color: transparent;"
        />
      </div>
    </div>
  </div>
</div>


